services:
  - type: web
    name: CammionControlApp
    env: node
    plan: free
    buildCommand: cd backend/src && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
